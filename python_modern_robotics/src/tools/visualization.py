import matplotlib.pyplot as plt
import numpy as np
import sympy as sym


class robot_visualization:
    def __init__(self, H_array, robot_config) -> None:
        self.H_array = H_array
        self.robot_config = robot_config
        
    def plotting_config(self):
        for i in the range(len(self.H_array)):
            pass
            